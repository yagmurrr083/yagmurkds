<!DOCTYPE html>
<html lang="tr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sabancı Vakfı KDS</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body class="bg-light">

    <div class="d-flex" id="wrapper">
        <!-- Sidebar -->
        <div class="bg-corporate border-end text-white" id="sidebar-wrapper"
            style="min-width: 250px; min-height: 100vh;">
            <div
                class="sidebar-heading text-center py-4 primary-text fs-4 fw-bold text-uppercase border-bottom border-secondary">
                <i class="fas fa-network-wired me-2"></i>Sabancı KDS
            </div>
            <div class="list-group list-group-flush my-3">
                <a href="/analiz"
                    class="list-group-item list-group-item-action bg-transparent text-white fw-bold <%= typeof activePage !== 'undefined' && activePage === 'analiz' ? 'active-sidebar' : '' %>">
                    <i class="fas fa-chart-line me-2"></i>ANALİZ
                </a>
                <a href="/firmalar"
                    class="list-group-item list-group-item-action bg-transparent text-white fw-bold <%= typeof activePage !== 'undefined' && activePage === 'firmalar' ? 'active-sidebar' : '' %>">
                    <i class="fas fa-building me-2"></i>FİRMALAR
                </a>
                <a href="/girisimciler"
                    class="list-group-item list-group-item-action bg-transparent text-white fw-bold <%= typeof activePage !== 'undefined' && activePage === 'girisimciler' ? 'active-sidebar' : '' %>">
                    <i class="fas fa-users me-2"></i>GİRİŞİMCİLER
                </a>
            </div>

            <div class="mt-auto p-3 text-center text-white-50 small">
                Sabancı Vakfı &copy; 2025
            </div>
        </div>

        <!-- Page Content Wrapper -->
        <div id="page-content-wrapper" class="w-100">
            <nav class="navbar navbar-expand-lg navbar-light bg-light border-bottom">
                <div class="container-fluid">
                    <button class="btn btn-primary" id="menu-toggle"><i class="fas fa-bars"></i></button>

                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                        data-bs-target="#navbarSupportedContent">
                        <span class="navbar-toggler-icon"></span>
                    </button>

                    <div class="collapse navbar-collapse" id="navbarSupportedContent">
                        <ul class="navbar-nav ms-auto mt-2 mt-lg-0">
                            <li class="nav-item">
                                <span class="nav-link text-muted">Hoşgeldiniz</span>
                            </li>
                        </ul>
                    </div>
                </div>
            </nav>

            <main class="container-xl py-4 fade-in-up">
                <%- include(body) %>
            </main>

            <footer class="text-center py-4 text-muted mt-auto border-top">
                <small>&copy; 2025 Sabancı Vakfı Karar Destek Sistemi. Tüm hakları saklıdır.</small>
            </footer>
        </div> <!-- /#page-content-wrapper -->
    </div> <!-- /#wrapper -->

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="/js/main.js"></script>
</body>

</html>